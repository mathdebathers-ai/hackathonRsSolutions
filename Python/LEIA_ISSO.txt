Na hora de iniciar o api.py use os seguintes comandos em ordem:
cd Python/
python -m venv venv
source venv/bin/activate
pip install python-dotenv
pip install google-generativeai
uvicorn api:app --host 0.0.0.0 --port 8000 --reload
